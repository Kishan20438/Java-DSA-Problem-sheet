
1161 Problem:-
Given the root of a binary tree, the level of its root is 1, the level of its children is 2, and so on.
Return the smallest level x such that the sum of all the values of nodes at level x is maximal.

Example 1:
Input: root = [1,7,0,7,-8,null,null]
Output: 2
Explanation: 
Level 1 sum = 1.
Level 2 sum = 7 + 0 = 7.
Level 3 sum = 7 + -8 = -1.
So we return the level with the maximum sum which is level 2.

Example 2:
Input: root = [989,null,10250,98693,-89388,null,null,null,-32127]
Output: 2
 
Constraints:
The number of nodes in the tree is in the range [1, 104].
-105 <= Node.val <= 105
 
===========================================================================================================================================================================================

Solution:-

💡 Intuition
The task is to find the level in a binary tree whose node values sum up to the maximum.
Since levels are clearly defined (root at level 1, its children at level 2, and so on), a level-order traversal is the most natural way to approach this problem.
Using Breadth-First Search (BFS) allows us to:
Traverse the tree level by level
Compute the sum at each level
Track the level with the maximum sum efficiently

🚀 Approach:-
Use a queue to perform BFS starting from the root.
Initialize the current level as 1.
For each level:
Process all nodes currently in the queue.
Calculate the sum of their values.
Add their left and right children to the queue.
Maintain:
max_val → maximum level sum found so far
max_level → level corresponding to max_val
Update the maximum whenever a higher sum is found.
Return the level with the highest sum.
(If multiple levels have the same sum, the smallest level is automatically chosen.)

⏱️ Complexity:-
Time Complexity: O(n)
Each node is visited exactly once.
Space Complexity: O(n)
In the worst case, the queue stores all nodes of a level.

Code:-
class Solution {
    public int maxLevelSum(TreeNode root) {
        Queue<TreeNode> q = new LinkedList<>();
        q.add(root);
        int max = Integer.MIN_VALUE;
        int ans = 1;
        int curr = 1;
        while (!q.isEmpty()) {
            int c = 0;
            int size = q.size();
            for (int i = 0; i < size; i++) {
                TreeNode temp = q.remove();
                c += temp.val;
                if (temp.left != null)
                    q.add(temp.left);
                if (temp.right != null) 
                    q.add(temp.right);
            }    
            if (c > max) {
                max = c;
                ans = curr;
            }
            curr += 1;
        }
 
